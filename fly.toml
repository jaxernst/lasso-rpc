# Minimal fly.toml for remote builds
# Actual deployment is managed via deployment/provision.mjs

app = "lasso-staging"
primary_region = "sjc"

[build]
  # Fly will use the Dockerfile in the repo root

[env]
  PORT = "4000"
  PHX_SERVER = "true"

# Note: This fly.toml is ONLY used for remote image builds
# The actual machine provisioning, regions, volumes, and services
# are configured via deployment/provision.mjs as per DEPLOYMENT_FLY_MACHINES_SPEC.md
